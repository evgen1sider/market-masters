name: CI

on:
  push:
    branches: [ develop, main ]
  pull_request:
    branches: [ develop, main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci

      - name: Check EOLs
        run: npm run check:eol

      - name: Run lint
        run: npm run lint

      - name: Run tests
        run: npm test

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov.info
          # Don't fail the entire workflow if the upload fails (e.g., token missing)
          fail_ci_if_error: false
          # The action accepts 'token' input; pass it only if the secret exists to avoid workflow validation issues
          token: ${{ secrets.CODECOV_TOKEN }}
          
